# Codebase Structure

**Analysis Date:** 2026-09-11

## Directory Layout

```
StewardMD/
├── index.html                 # Main application entry point (PWA root)
├── sw.js                      # Service worker for offline caching & background sync
├── package.json               # Dependencies, scripts, and Capacitor configuration
├── capacitor.config.json      # Capacitor runtime configuration
├── wrangler.toml              # Cloudflare worker deployment configuration
│
├── surgx/                     # Surgical Intelligence domain
│   ├── surgx-screens.js       # Surgical UI view controllers
│   ├── surgx-store.js         # Local surgical note storage
│   ├── surgx-backup.js        # Encrypted backup & restore
│   ├── surgx-model.js         # Operative note schemas & generators
│   └── surgx.css              # Surgical module visual themes
│
├── sknx/ & sknx-*.js          # Dermatology & Skin Vision domain
│   ├── sknx.js                # Core controller
│   ├── sknx-vision.js         # On-device vision pipeline
│   ├── sknx-screens.js        # Dermatology UI
│   └── sknx-compare.js        # Longitudinal lesion comparisons
│
├── thorex-*.js                # Thorax & Chest X-ray domain
│   ├── thorex.js              # Core controller
│   ├── thorex-ort.js          # ONNX Runtime model inference
│   └── thorex-report.js       # Automated radiology report generator
│
├── wardsynq/ & wardsynq-*.js  # Inpatient ward rounding domain
│   ├── ward.js                # Ward dashboard & bed manager
│   ├── wardsynq-alert-ui.js   # Real-time clinical alert indicators
│   └── wardsynq-ghis-*.js     # GHIS hospital system bridge
│
├── voice-*.js & voice-service/# Ambient clinical scribe domain
│   ├── voice.js               # Audio recording orchestrator
│   ├── voice-vitals.js        # NLP vitals parser
│   └── voice-emr-map.js       # FHIR/EMR schema field mapper
│
├── local-plugins/             # Custom native Capacitor plugins
│   ├── capacitor-llama/       # On-device local LLM engine
│   ├── capacitor-whisper/     # On-device speech recognition
│   ├── capacitor-sknx-vision/ # Native vision AI acceleration
│   ├── capacitor-ecg-digitiser# Paper ECG vectorizer
│   └── capacitor-watch-bridge # Smartwatch bridge
│
├── ios/                       # Native Xcode workspace (App, Extensions)
├── android/                   # Native Android Studio project
├── test/                      # Node.js native test suite
│   ├── connect/               # Teleconsultation tests
│   ├── surgx-*.test.mjs       # Surgical note & backup tests
│   └── *.test.mjs             # Unit and integration tests
│
├── scripts/                   # Build, sync, and evaluation scripts
│   ├── build-www.sh           # Assembles web bundle into www/
│   └── maik-eval.mjs          # Clinical reasoning evaluation suite
│
└── www/                       # Built static bundle copied into native apps
```

## Key Architectural Locations

- **Entry Point:** `index.html` (loads stylesheets and modules in verified deterministic dependency order)
- **Clinical Database:** Handled via SQLite abstraction layer in `store-assets/` and `vault/`
- **Native Bridges:** Located in `local-plugins/` with platform-specific Swift and Kotlin code
- **Configuration & Flags:** `sknx-flags.js`, `surgx-flags.js`, `wardsynq-flags.js`, `steward-id-flags.js`

---
*Generated by GSD Codebase Mapper*
