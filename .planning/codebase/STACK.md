# Technology Stack

**Analysis Date:** 2026-09-11

## Languages

**Primary:**
- JavaScript (ES Modules & browser vanilla JS) — Web client, clinical decision support modules, service workers, tests
- Swift (iOS) — Native Capacitor plugins (`local-plugins/*`, iOS app shell)
- Java / Kotlin (Android) — Native Capacitor plugins (`local-plugins/*`, Android app shell)

**Secondary:**
- HTML5 / CSS3 — UI layouts, design system, clinical forms, surgical diagrams, print styles
- Bash / Shell — Build and sync automation (`scripts/build-www.sh`, CI scripts)
- SQL / SQLite — Local clinical database (`@capacitor-community/sqlite`)

## Runtime

**Environment:**
- Node.js (v20+ / v26.4.0) — Test runner (`node:test`), evaluation scripts, build tooling
- Web Browser / WebView — PWA execution runtime
- iOS 16+ / Android API 33+ — Capacitor native mobile runtime

**Package Manager:**
- npm
- Lockfile: `package-lock.json` present

## Frameworks

**Core:**
- Capacitor 8 (`@capacitor/core`, `@capacitor/ios`, `@capacitor/android`) — Cross-platform native bridge
- PWA (Buildless vanilla JS + Web Components / modern DOM) — Zero-bundler client architecture assembled via `scripts/build-www.sh` into `www/`

**Testing:**
- Node.js native test runner (`node:test`, `node:assert`) with `--experimental-test-module-mocks` and `--experimental-sqlite`
- Custom evaluation suites (`scripts/maik-eval.mjs`, `scripts/maik-answer-eval.mjs`)

**Build/Dev:**
- `scripts/build-www.sh` — Static assets and JS bundle assembler into `www/`
- `patch-package` — Post-install package patches
- `npx cap sync` — Capacitor platform sync

## Key Dependencies

**Critical Native & Device Capabilities:**
- `@capacitor-community/sqlite` (^8.1.0) — Offline-first local clinical data storage
- `@capacitor-firebase/authentication` (^8.3.0) — User identity & authentication
- `@capacitor-firebase/app-check` (^8.3.0) — Device and app integrity verification
- `@aparajita/capacitor-biometric-auth` (^10.0.0) — Fingerprint & FaceID login
- `capacitor-secure-storage-plugin` (^0.13.0) — Encrypted key/credential storage
- `@capgo/capacitor-updater` (^8.51.14) — Over-the-air (OTA) dynamic hot updates

**Local Custom Capacitor Plugins:**
- `@stewardmd/capacitor-llama` (`local-plugins/capacitor-llama`) — On-device local LLM execution
- `@stewardmd/capacitor-whisper` (`local-plugins/capacitor-whisper`) — On-device clinical speech transcription / scribe
- `@stewardmd/capacitor-sknx-vision` (`local-plugins/capacitor-sknx-vision`) — On-device skin lesion / dermatology vision AI
- `@stewardmd/capacitor-ecg-digitiser` (`local-plugins/capacitor-ecg-digitiser`) — ECG strip digitization and analysis
- `@stewardmd/capacitor-fundx-depth` (`local-plugins/capacitor-fundx-depth`) — Fundus photography depth analysis
- `@stewardmd/capacitor-watch-bridge` (`local-plugins/capacitor-watch-bridge`) — Smartwatch communication bridge
- `@stewardmd/capacitor-iap` (`local-plugins/capacitor-iap`) — In-app purchases & tier entitlements

**Animation & UI Utilities:**
- `motion` (^12.43.0) — Smooth UI animations
- `fflate` (^0.8.3) — High-performance decompression for model weights and backups

## Configuration

**Environment:**
- `.stewardmd-secrets.env`, `wrangler.toml` for Cloudflare workers
- Firebase configurations (`google-services.json`, `GoogleService-Info.plist`)
- Service configuration flags in `sknx-flags.js`, `surgx-flags.js`, `thorex-flags.js`, `wardsynq-flags.js`

**Build:**
- `capacitor.config.json` / `capacitor.config.ts` — Capacitor runtime and plugin configurations
- `package.json` scripts: `npm run sync`, `npm run ios`, `npm run android`, `npm test`

---
*Generated by GSD Codebase Mapper*
