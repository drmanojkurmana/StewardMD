# WardSynQ live test, 2026-09-15

Tester: Claude (browser QA, chrome-devtools). Site: https://wardsynq.com/ . Hospital: WardSynQ DEMO Superspecialty Hospital (SMD-6TEQZM), role Doctor, admin.
Test data: fake patients named "Test Patient QA-xx" only.

## Summary

41 findings: 4 blocker, 23 major, 14 minor. Test patients created: Test Patient QA-01 (SMD-6TEQZM-00027, admitted, transferred, discharged home) and Test Patient QA-02 (SMD-6TEQZM-00028, ED arrival, admitted to GAS-03, still admitted). Nothing changed in Admin Center or sign-in security.

| ID | Sev | Screen | Problem |
|----|-----|--------|---------|
| LT-01 | blocker | `#/opd` | OPD desk leaves WardSynQ for a StewardMD sign-in page; OPD journey impossible |
| LT-09 | blocker | Order safety workstation | Hard-coded fake patients; signed orders never saved anywhere |
| LT-14 | blocker | Chart > medication order / round | Order saved with `safety: null`; no doses ever appear on the eMAR round |
| LT-30 | blocker | `#/ward/cashier` | Nothing priced: Raise invoice silently does nothing; no invoice, no payment |
| LT-02 | major | Bed board | Admit form opens 2000 px below the clicked bed |
| LT-04 | major | Ward home | Every ward: alerts reach nobody on duty; 403 on duty status |
| LT-06 | major | Chart | Late re-render wipes typed vitals |
| LT-08 | major | Fluid balance | No urine output; output uses intake routes |
| LT-10 | major | Order workstation rules | Paracetamol 1 g hard stop in 62 kg adult; wrong duplication warning |
| LT-11 | major | Fluid balance | IV intake saved as oral |
| LT-12 | major | Chart | Re-render wipes half-typed medication order |
| LT-16 | major | Pathways | 500 error hidden behind "no pathways" |
| LT-17 | major | Discharge / Follow-up | One click opens a browser prompt; discharge destination is free text |
| LT-20 | major | Pharmacy | Prescriber verifies own order; list does not refresh |
| LT-21 | major | Nurse worklist | ~60 s load; only first 60 patients, test patient missing |
| LT-22 | major | Shift handover | Raw ids instead of patient and staff names |
| LT-23 | major | Chart | No way to create a handover or nursing task |
| LT-25 | major | Lab board | No Collect; result released for uncollected specimen |
| LT-26 | major | Critical results counts | Map 1 vs lab board 5-6; wrong minutes open |
| LT-27 | major | Radiology board | Waiting times +5 h 30 min (timezone); reported study stays requested |
| LT-28 | major | Critical results board | No names, uid as acknowledger, time resets to 0, escalation reached nobody |
| LT-31 | major | Discharge summary | Released results missing/"open"; first Sign fails (404 no_draft) |
| LT-32 | major | Discharge | Discharge allowed with no bill and open orders |
| LT-34 | major | System health | Document storage down, MaiK down, no backups ever |
| LT-36 | major | Staff rota | No shifts; phone number shown as staff name |
| LT-38 | major | Reports | Raw 51k-character data dump, no reports |
| LT-40 | major | MaiK | Ask MaiK -> 502, screen shows "bad_response" |
| minor | | | LT-03, 05, 07, 13, 15, 18, 19, 24, 29, 33, 35, 37, 39, 41 (details below) |

### Journeys
1. Registration: done (find by MRN, register QA-01 and QA-02; duplicate-mobile check works). Mobile number is mandatory; used 9999999999.
2. OPD: NOT done, blocked by LT-01.
3. Admission: done (bed board admit, transfer GAS-03 -> GMA-11), with LT-02/LT-05.
4. Inpatient ward: partly. Chart tabs all opened; vitals, template note, fluids (wrongly stored), chart prescription, pharmacy verify done. Order safety workstation cannot order for a real patient (LT-09). eMAR dose, nursing task and handover NOT done (LT-14, LT-23).
5. Lab and radiology: done with defects (LT-25, LT-27); CXR correctly on Radiology board with no Collect button.
6. Critical result: done (created via Hb 5.2, acknowledged) with LT-28.
7. ED: done (arrival, triage, admit) with LT-29.
8. Billing: invoice and payment NOT done (LT-30). Patient copy print called (LT-33), discharge summary signed, discharge done.
9. Admin and command screens: all opened read-only (LT-34 to LT-40).
10. Language: done (LT-41, known).

### What worked
Registration and MRN lookup; duplicate-mobile warning; bed board admit and transfer (server writes 200); vitals and flowsheet; template progress note; investigation ordering; pharmacy verification; lab result release with automatic critical-result alert; radiology report release; critical result acknowledgement with a reason; ED arrival/triage/admit; discharge summary sign (after draft) and discharge; Admin Center, Integration, Accounts, Group, Sign-in security pages load with no failed requests; order-safety allergy hard stop (amoxicillin vs penicillin); language switch persists and reverts cleanly. No 401 anywhere.

## Findings (appended in test order)

### LT-01 (blocker) OPD desk opens a second sign-in wall (StewardMD OPD Console)
- Screen: `#/opd` (sidebar "OPD desk", Map tile "OPD desk", or typing the URL) -> redirects to `https://wardsynq.com/opd`, title "StewardMD OPD Console".
- Steps: signed in to WardSynQ as Doctor/admin in DEMO hospital; click "OPD desk" in the sidebar or on the Map.
- Expected: WardSynQ OPD queue for the current hospital, no new sign-in.
- Actual: leaves WardSynQ for a StewardMD-branded sign-in page (Owner/Doctor: Google or email+password; Staff: clinic code+PIN). The existing Firebase session is not used. No console error.
- Impact: whole OPD journey (check-in, token, consult, prescription, investigations, results) cannot be done from WardSynQ.
- Screenshot: /Users/diwakarkumar/.claude/jobs/d1986003/live-test/LT-01-opd-signin-wall.png

### LT-02 (major) Bed board: clicking a free bed shows nothing; the admit form opens off screen
- Screen: `#/ward/board`
- Steps: open Bed board, click free bed GAS-03.
- Expected: admit sheet opens next to/over the bed.
- Actual: bed only gets a highlight ring. "Admit to Gastroenterology Ward, bed GAS-03" form is rendered ~2050 px down, below all 16 wards (viewport 788 px). No scroll, no focus move. A nurse will think the click did nothing.
- Console/network: none.

### LT-03 (minor) Bed board demo data: same names in several beds, male names in Maternity
- Screen: `#/ward/board`
- Actual: e.g. "Demo Arjun Synthia" shown in GMA-09, ICU-04 and OBG-03 (Maternity); "Demo Omar Samplewala" in GSA-03, HDU-01, OBG-07; "Demo Manav Fictor" in CAR-04, EMO-07, ONC-01. The ward list shows these are different MRNs (e.g. CAR-04 SMD-DEMO-00022, EMO-07 SMD-DEMO-00082), so it is the demo seed reusing names, not a double admission. Still looks like a double admission to anyone shown the demo; the bed board shows no MRN to tell them apart.
- Also: ward "cardio" with "Bed list not configured" and "Demo Naina Synthia (no bed assigned)", while the same name is in NEU-04. Beds are listed out of order (CAR-02, CAR-04, CAR-06, CAR-08, CAR-03...).
- Also: PUL-04 holds "B VENKATA RAMANA", an all-caps real-looking name in the DEMO hospital. Check it is not real patient data.

### LT-04 (major) Every ward: "level 2 alert would reach nobody on duty"; admin gets 403 on duty status
- Screen: ward home (`#/ward/`), shown after admitting.
- Actual: all 16 wards show "level 2 alert would reach nobody on duty (0 nurses, 0 residents, 0 consultants)". Escalations in the demo hospital go to nobody. The page also calls duty status for the signed-in user and gets `GET /api/queue/roster/duty-status` 403 `not_ward_team` ("Your role here is admin"), although the header says "Doctor, admin". Console: "Failed to load resource: 403".
- Expected: demo roster seeded with staff on duty; no call (or a quiet no-op) for roles that cannot go on duty.

### LT-05 (minor) After admitting from the bed board the screen jumps to the ward home, scrolled to the middle, URL stays `#/ward/board`
- Screen: `#/ward/board`
- Steps: pick free bed GAS-03, find MRN, click "This Is The Patient - Admit".
- Actual: `POST /api/queue/ward/admit` 200 (good, patient appears in GAS-03), but there is no confirmation toast; view switches to ward home scrolled to the duty banners, hash still `#/ward/board`. The "Admission type" dropdown (General/ICU/Maternity...) is not sent in the admit request body. Button label is Title Case "This Is The Patient - Admit".

### LT-06 (major) Chart re-renders a few seconds after opening and wipes vitals already typed
- Screen: inpatient chart, Overview (hash stays `#/ward/board`, the chart has no URL of its own).
- Steps: open Test Patient QA-01 from the ward list; straight away type Systolic 120, Diastolic 80, Pulse 88, Resp 18.
- Expected: typed values stay.
- Actual: while typing, the chart re-rendered (late sections loading); all four values were blank again and the later fields were replaced by new elements. Focus stayed in the Resp rate box, so the nurse keeps typing into an emptied form. Second attempt after the page settled saved fine.
- Console/network: none failing.

### LT-07 (minor) Vitals/NEWS2 display text
- Screen: chart Overview, Vitals and Flowsheet.
- Actual: before any vitals, NEWS2 reads "Incomplete: RespiratoryRate, OxygenSaturation, SupplementalOxygen, SystolicBloodPressure, Pulse, Consciousness, Temperature Were Not Recorded, So The Partial Total Of 0 Is Not A Risk Assessment And Must Not Be Read As One" (code names, every word capitalised). Flowsheet shows raw units "mm[Hg]", "Cel". "Record vitals" clears the form at once but `POST /api/queue/ward/vitals` stays pending ~5-8 s with no spinner; flowsheet only updates after. Fluid balance for a patient admitted 30 min earlier says "13 of the last 13 hours have nothing charted".

### LT-08 (major) Fluid balance: cannot chart urine output; Output keeps intake routes
- Screen: chart Overview, Fluid balance.
- Steps: choose "Output" in the first dropdown; open the second (route) dropdown.
- Expected: Urine, Drain, Vomit, Stool, Blood loss, Other.
- Actual: route dropdown still offers Oral / IV / NG / Blood / Other. The output routes (Drain, Vomit, Stool, Blood loss, Other) are stuffed into the Intake/Output dropdown instead, and "Urine" exists nowhere, although the help text says "Output includes urine". A nurse can save "Output, Oral, 300 mL". Intake 500 mL updated the totals (IN 500, balance +500) but was stored as Oral, see LT-11.
- Screenshot: /Users/diwakarkumar/.claude/jobs/d1986003/live-test/LT-08-fluid-output-routes.png

### LT-09 (blocker) Order safety workstation is a disconnected demo: fixed fake patients, orders never saved
- Screen: Map tile "Order safety workstation" -> `/wardsynq/ui/wardsynq?site=1&demo=1`
- Steps: open it from the Map in the DEMO hospital; look for Test Patient QA-01; sign paracetamol 650 mg for the shown patient.
- Expected: the hospital's own admitted patients; a signed order lands on the patient's chart/eMAR.
- Actual: only three hard-coded patients (Anjali Menon MICU 04, Ravi Deshpande Ward 3A bed 12, Meera Iyer Paediatrics 02) that do not exist in the hospital. Test Patient QA-01 cannot be selected. "Sign order" says "Signed and kept with the record" but no network request is made (only the rules JSON files load); nothing reaches a chart. The order has no frequency or duration field. Notes and Handover tabs are disabled.
- Safety checking itself loads (no 401): amoxicillin gives the penicillin allergy hard stop.
- Screenshot: /Users/diwakarkumar/.claude/jobs/d1986003/live-test/LT-09-order-workstation-fake-patients.png

### LT-10 (major) Order safety rules: wrong hard stop and wrong duplication warning
- Screen: `/wardsynq/ui/wardsynq?site=1&demo=1`, patient Anjali Menon (67 F, 62 kg).
- Actual 1: paracetamol 1000 mg oral = "Hard stop ... exceeds the weight-based ceiling (15 mg/kg x 62 kg = 930 mg). There is no override." 1 g is a standard adult dose above 50 kg; paediatric mg/kg ceiling applied to an adult, with no override.
- Actual 2: amoxicillin shows "Monitor: amoxicillin with Warfarin 3mg, Clarithromycin 500mg. Possible therapeutic duplication". Warfarin is not a duplicate of amoxicillin (it is an INR interaction). The message also uses an em-dash in app text.
- Banner says content is unapproved seed data; logging anyway because a clinician will see it.

### LT-11 (major) Fluid balance saves every intake as "Oral", whatever route is picked
- Screen: chart Overview, Fluid balance.
- Steps: Intake, route "IV" (select value confirmed `iv` in the DOM), 100 mL, click Chart.
- Expected: IV 100 mL.
- Actual: `POST /api/queue/ward/fluid` 200 with body `{"direction":"intake","kind":"oral","value":"100"}`; server stores `intake.oral`. The earlier 500 mL IV entry also shows as "ORAL 500 mL" on the flowsheet. IV fluids are charted as oral intake.

### LT-12 (major) Chart keeps re-rendering after open and wipes a half-typed medication order
- Screen: chart Overview, New medication order.
- Steps: open the chart, wait ~9 s, type Drug "Paracetamol", Dose 650, then Unit.
- Actual: after Dose the form re-rendered; Drug and Dose blank again, Unit/Route/Frequency/Prescribe "did not become interactive". Same root as LT-06 but much later than first load, so waiting a few seconds is not a workaround.

### LT-13 (minor) "Your notes, not yet submitted" list has no patient name, shows raw slugs
- Screen: ward home `#/ward/`.
- Actual: rows read "discharge-summary complete Submit", "progress complete Submit", "progress complete Submit". No patient, bed, or time, so a doctor cannot tell which note they are submitting. Template ids shown instead of names.

### LT-14 (blocker) Chart medication order saves with no safety check and never reaches the eMAR round
- Screen: chart Overview, New medication order -> Medication round.
- Steps: Paracetamol 650 mg oral TDS, Prescribe; then Medication round "Load" for today.
- Expected: server safety/allergy check result shown; three TDS doses due today on the round.
- Actual: `POST /api/queue/ward/medication-order` 200 with `"status":"active","safety":null,"formulary":"not-configured"`. The form says "Every safety and formulary check happens on the server" but no check ran and nothing tells the doctor. No confirmation shown. `GET /api/queue/ward/schedule` returns `"due":[],"prn":[],"unscheduled":[]`; round says "No doses fall in this window". eMAR dose cannot be given, so the eMAR journey is blocked.

### LT-15 (minor) Investigations: free-text tests, status "NONE", list lags
- Screen: chart Overview, Investigations.
- Actual: test is free text; request sends `code` = the typed words, no catalogue. After ordering, "On order" is empty for several seconds, then shows "Complete blood count laboratory NONE" with a Collect button ("NONE" is a raw status). The Reason field vanished on a later re-render.

### LT-16 (major) Pathways screen: server error 500
- Screen: chart > Overview > Pathways.
- Actual: page says "No active clinical pathways" but `GET /api/queue/ward/pathway-progress?patientId=...` returns 500 `{"ok":false,"error":"opd_org: id required"}`. The error is hidden, so "no pathways" may be false.

### LT-17 (major) "Discharge" and "Follow-up" chart buttons pop a browser prompt box; discharge destination is free text
- Screen: chart > Documents & discharge > Discharge; chart > Orders > Follow-up.
- Steps: click the "Discharge" sub-tab (a navigation item, next to "Summary" and "Patient copy").
- Expected: a discharge screen/form with a fixed choice of destination and a confirm step.
- Actual: one click immediately opens a native browser prompt "Where did Test Patient QA-01 go? Type: home, transferred, left-against-advice, died". Destination (including "died") is typed free text. Cancel shows a red toast "Say where the patient went..." Follow-up does the same ("Why should this patient be seen again?"); after cancelling, the red "A follow-up needs a reason." toast stays on every later chart screen until closed. Native prompts are also blocked/ugly on mobile WebViews.

### LT-18 (minor) Chart sub-tabs: dead buttons, disappearing tab, raw ids, missing Back label
- Screen: inpatient chart.
- "Specialty" (under Specialty) does nothing when clicked. "Summary" (under Documents & discharge) looks dead but actually opens the Discharge summary as a separate full-screen layer that stays on top and swallows clicks on later chart screens (see LT-19).
- "Drips" was listed under Orders on first open, then disappeared from the Orders tab later in the same session.
- Oncology, Cardiology, Radiology, Pharmacy and Blood bank screens show the internal id "opd-pat-smd-6teqzm-00027" under the title instead of the patient name/MRN.
- Immunizations/Forms screens' back arrow has no accessible name (other screens use "Back").
- Opened fine: Timeline, Care plan, Chart check, Contacts, Order sets (none configured), Medicines on arrival, Referrals, Wristband, Wounds, Risk (no tools configured), Immunizations, Forms, Operations, Oncology, Cardiology, Radiology (CXR on worklist), Pharmacy (paracetamol UNVERIFIED), Blood bank, Documents, Consent, IPS summary, Transfer, Patient copy, Billing, TPA, ROI.

### LT-19 (minor) Discharge summary layer: stays on top, icon names as text, UTC times, Report Bug covers "Sign and finalise"
- Screen: chart > Documents & discharge > Summary.
- Actual: opens a light-theme full-screen layer over the dark chart; it is not closed when you pick another chart screen, so clicks meant for the Pharmacy "Verify" button went to it. Status card shows icon ligature names as words ("auto_awesome_motion", "person"). "Admission and stay" reads "Admitted: 2026-09-15T15:23:31.058Z" (raw UTC ISO; header says 20:53). Clinical course line also raw ISO. The floating red "Report Bug" button sits on top of "Sign and finalise". Outstanding list correctly shows CBC, CXR, paracetamol still open.
- Screenshot: /Users/diwakarkumar/.claude/jobs/d1986003/live-test/LT-19-discharge-summary-overlay.png

### LT-20 (major) Pharmacy verification: prescriber can verify own order; screen does not refresh; eMAR still empty after verification
- Screen: chart > Specialty > Pharmacy.
- Steps: same account that prescribed paracetamol (Doctor, admin) picks the order, clicks Verify.
- Actual: `POST /api/queue/ward/verify-order` 200 `outcome: verified`. No separation of duties: the prescriber verified their own prescription with no warning. The list still says "UNVERIFIED" until you leave and come back. After verification the order shows under "Active medications" (before, it said "Nothing currently active" while the order was active but unverified, with no "awaiting pharmacy" hint), but Medication round > Load still says "No doses fall in this window" for a TDS order today. eMAR dose could not be given (continues LT-14).

### LT-21 (major) Nurse worklist takes about a minute to load and hides patients after the first 60
- Screen: `#/ward/nurseworklist` (Map > Nurse worklist).
- Actual: "Loading the ward..." for ~60 s (`GET /api/queue/ward/nurse-worklist` pending that long, no error). Then "Only the first 60 patients on this ward are shown." with no search or paging; Test Patient QA-01 (GMA-11, with an active verified TDS paracetamol) is not in the list at all. "Next due: 53 overdue · 0 in the next 4 hours": demo doses are all overdue. Every card: "No nurse assigned · No observation frequency set".
- Transfer (journey 3) worked: chart > Documents & discharge > Transfer > GMA-11 > confirm; `POST /api/queue/ward/transfer` 200, toast "Moved to General Medicine A, bed GMA-11". Confirmation is a native browser confirm box.

### LT-22 (major) Shift handover list shows internal ids instead of patient names and staff
- Screen: `#/ward/handovers`
- Actual: each card is titled "WAITING TO BE TAKEN opd-pat-smd-demo-00001" (no patient name, MRN only inside the id) and "given by fb:DcGIzIXwxURU0G9L4J5jehluENl1" (raw sign-in uid, not a person). A nurse taking over cannot see who the patient is or who handed over without opening something else. 6 demo handovers from 01-02 Sept are still "waiting".

### LT-23 (major) No way to hand a patient over or add a nursing task from the chart
- Screen: `#/ward/handovers` says "Open a patient from the ward list to hand them over."; inpatient chart (all six tabs, Workspace, Consultation).
- Actual: no button, tab or sub-tab in the chart for "handover", "SBAR" or "task" (searched every button/tab in the DOM). The only "Record handover" is under Documents & discharge > Patient copy and means handing the summary to the patient's portal. Nurse worklist cards show "0 open tasks" but give no way to add one. Shift handover and nursing task journeys could not be done.

### LT-24 (minor) Patient copy / Workspace text problems
- Patient copy (chart > Documents & discharge > Patient copy): shows date of birth "01 Sep 1981" for a patient registered with age 45 only (a made-up DOB on a patient-facing page); shows the config key "This hospital has not configured wardsynq.neverRelease...".
- Workspace: "Latest observations: No score yet" although vitals were charted and the flowsheet shows NEWS2 0 Low risk. "Waiting for a result" rows read `a clinician account ordered Chest X-ray PA view (imaging) — active WAITING` (no clinician name, em-dash, raw status).

### LT-25 (major) Lab board: no Collect action; a result can be released for a specimen never collected, and it then stays "Awaiting collection"
- Screen: `#/ward/labboard`
- Steps: CBC for Test Patient QA-01 ordered on the ward, not collected. On the lab board it is listed BOTH under "Awaiting collection · 3" and "Awaiting a result · 4". Click Enter result, Haemoglobin 5.2 g/dL (13-17), Final, Release result.
- Expected: lab can mark the specimen collected/received; result entry only after receipt; after release it leaves every "awaiting" list.
- Actual: "Awaiting collection" rows are plain text, no Collect/Receive button (collection only exists on the ward chart). Release succeeded ("1 critical value in this result. The critical-result alert has been opened."), CBC left "Awaiting a result" but is still under "Awaiting collection" after refresh. No specimen, accession or collected-time is ever recorded.
- Also on this board: an imaging order "CXR" (Demo Farid Synthia) sits under laboratory "Awaiting a result" with Enter result / Culture / Histopathology buttons; an orphan "BLOOD alcohol LEVELS URGENT, not on the ward list". The result form opens inline at the top with 10 blank rows and no abnormal/critical flag column (critical detection worked anyway).
- Board needs manual refresh: after release the lists did not update until reload; clicking Refresh failed once because the element had already been re-rendered.

### LT-26 (major) Critical results counts disagree and "minutes open" is wrong
- Screens: Map tile "Critical results 1 open"; ward home "Critical results not acknowledged 1"; lab board "Critical results · 5" (6 after my test result).
- Actual: 4 of the demo criticals (Sodium 118, Creatinine 8.2) show "0 min open" although they are seeded days ago; one shows "5588 min open" (should read ~3.9 days). The hospital-wide count on the Map and ward home ignores most of them.

### LT-27 (major) Radiology board: waiting times off by 5 h 30 min; reported study stays on the "Requested" list
- Screen: Radiology board (opened from the lab board button; hash stays `#/ward/labboard`).
- Worked: the chest X-ray is on the Radiology worklist with only "File Report" (no Collect button); report filed as CR, Final, findings + impression; "Released."; "Reported this session · 1 ... CR · final · 15 Sept, 21:55".
- Actual 1: Test Patient QA-01's CXR, ordered ~40 min earlier (21:06 IST), shows "waiting 6h 9m". The extra 5 h 30 min is the IST offset (UTC time read as local). All waiting times on the board are inflated the same way.
- Actual 2: after release, the study is still listed under "Requested · 7" with a File Report button, so it can be reported twice.
- Actual 3: report form re-rendered ~2 s after opening ("Checking for images..." finished) and the typed Modality/Findings/Impression and the Release button were replaced; had to type again (same pattern as LT-06). Every study shows "modality not mapped". Screen shows the config key "wardsynq.imagingViewer.urlTemplate". The lab board's red critical toast is still showing on the Radiology board.

### LT-28 (major) Critical results board: no patient names, acknowledger shown as a raw uid, "min since reported" resets to 0 on acknowledge, escalation reached nobody
- Screen: `#/ward/critsboard`
- Steps: find Haemoglobin 5.2 g/dL (Test Patient QA-01, released from lab 3 min earlier), click Acknowledge, answer the prompt "What did you do about this result?".
- Worked: result appears within minutes with "OUTSIDE CRITICAL LIMIT"; acknowledgement saved; toast "Acknowledged. Next: Open chart...".
- Actual: every card identifies the patient only as "opd-pat-smd-6teqzm-00027" / "opd-pat-smd-demo-00056" (no name, bed or ward). After acknowledging, the card says "0 min since reported · acknowledged by fb:DcGIzIXwxURU0G9L4J5jehluENl1" (time since reported reset to 0; person shown as a sign-in uid). The header still says "Open loops · 6" with 5 of 6 already acknowledged. Oldest creatinine 8.2: "5592 min since reported · ESCALATE ... Escalated 13 Sept after 2771 min: nobody was notified (no notification channel is set up)". Acknowledge uses a native browser prompt.

### LT-29 (minor) Emergency department journey: works, with rough edges
- Screen: `#/ward/edboard`
- Worked: registered Test Patient QA-02 (SMD-6TEQZM-00028; duplicate-mobile warning shown correctly), ED Arrival by MRN (`POST ed-arrival` 200, "Arrived."), triage 3 - Urgent (`POST ed-triage` 200), Disposition Admit > bed GAS-03 (`POST ed-disposition` 200, "Disposition: admitted."); bed board shows QA-02 in GAS-03.
- Rough edges: (1) the known-patient arrival has no chief complaint field; the ED list says "No chief complaint recorded". (2) Triage is only an acuity number; "Triaged ... by fb:DcGIzIXwxURU0..." shows a raw uid. (3) The ED patient screen re-rendered ~2 s after opening and the Acuity/Record triage controls "did not become interactive" on the first try (LT-06 pattern). (4) Clicking a free bed during ED admit admits immediately with no confirmation, while ward Transfer asks a confirm; one mis-click puts the patient in the wrong bed. (5) The "Test Patient QA-02 added" success modal from the Patients page stayed open on top of the ED board after navigating, blocking the Find button; the text "Registered SMD-6TEQZM-00028" also stays at the bottom of the ED board. (6) Registering on the Patients page is "Add to queue" (adds the patient to the OPD queue even when they are going to ED).
- Navigation: from a ward board (e.g. Critical results), clicking another ward board in the sidebar, or changing the hash to one, lands on the Map (`#/home`) instead; you have to click again.

### LT-30 (blocker) Billing: "Raise invoice" silently does nothing; no bed, pharmacy or test prices in the demo hospital, so no invoice and no payment possible
- Screen: `#/ward/cashier`
- Steps: Look up SMD-6TEQZM-00027 (admitted 1 h+, bed, paracetamol, CBC, CXR) > "Raise invoice from today's charges".
- Expected: invoice with bed/day, investigations, medicines; then post a payment.
- Actual: "Not billed yet: NO PRICE SET Complete blood count / NO PRICE SET Chest X-ray PA view"; no bed or medicine charge listed at all. Clicking Raise invoice: `POST /api/queue/ward/invoice` 200 `{"written":0,"skipped":"nothing_priced","detail":"Nothing chargeable is priced right now."}`; the screen shows nothing (no toast, still "No invoices for this patient yet"). "Post an amount" applies to "whichever invoice button above you use", and there is no invoice, so no payment/deposit could be recorded.

### LT-31 (major) Discharge summary misses released results and keeps them "open"; first "Sign and finalise" fails
- Screen: chart > Documents & discharge > Summary (full-screen layer).
- Actual 1: Investigations section lists "Complete blood count (active)" and "Chest X-ray PA view (active)", and Outstanding lists both as "INVESTIGATION OPEN", although the CBC result (Hb 5.2 g/dL, critical, acknowledged) and the CXR report were released an hour earlier. The critical haemoglobin appears nowhere in the summary. Medications: "doses administered on this admission: 0" (eMAR never offered a dose, LT-14).
- Actual 2: Status card says "Not yet drafted ... You may sign this". Clicking Sign and finalise (after the "cannot be edited" confirm) -> `POST /api/queue/ward/sign-discharge-summary` 404 `no_draft`; red banner in developer wording "draft the discharge summary before signing it". You must press Save draft first (`POST discharge-summary` 200), but the status card still says "Not yet drafted" after saving. Second sign -> 200 "Signed. This version is now immutable."
- Accessible names of the footer buttons include icon words: "print Print", "save Save draft", "verified Sign and finalise".

### LT-32 (major) Discharge goes through with no bill, open orders and pending "results", via free-text prompt
- Screen: chart > Documents & discharge > Discharge.
- Steps: after signing the summary, click Discharge, type "home", confirm.
- Actual: "Stay ended. The bed is free." No check that there is no invoice/payment (LT-30), that paracetamol is still active, or that investigations are shown open. The first attempt with the browser prompt left empty/typed through the dialog gave only a red "Say where the patient went..." toast (see LT-17). Ward status after discharge reads "87 patients · 85 beds occupied" (was 86/84 before the ED admission; should be back to 86/84).

### LT-33 (minor, not visually confirmed) Patient copy print likely prints the whole Map page too
- Screen: chart > Documents & discharge > Patient copy > Print.
- Actual: Print calls the browser print (window.print called once). The ward print stylesheet only restyles `#smdWard`; unlike the discharge summary stylesheet it does not hide the rest of the page (sidebar, Map tiles). Print preview itself could not be captured with the test tools, so check on a real printer/PDF.

### LT-34 (major) System health: document storage DOWN, MaiK DOWN, no backup ever recorded
- Screen: `#/admin` > System health.
- Actual: "3 of 9 dependencies are not fully up": Document storage Down ("Storage refused the put step of a test save (status 400)" - uploads fail and existing documents cannot be opened); MaiK clinical AI Down ("No model provider answered (Google model service)"); Backup and restore test Down ("No backup run has ever been recorded. No restore test has ever been recorded."). Times are raw UTC ISO strings ("2026-09-15T16:45:50.537Z").
- Not tested further: Documents upload in the chart (would fail per this check).

### LT-35 (minor) Admin Center read-only pass: config gaps and internal text
- Screen: `#/admin`, all 17 tabs opened; no console errors, no failed requests.
- Price list: "No prices set yet" (root cause of LT-30). Rooms: "No consulting rooms yet". Forms, Clinical pathways, Safety reminders: none published. Clinical seed data: "70 of 70 items unapproved".
- Hospital tab shows internal ids (org id, connect tenant) and the sentence "Default ladder approved by Dr Manoj Kurmana on 2026-09-14 (owner decision O5)" to every hospital admin. OPD token prefixes are listed for administrative departments (Billing & TPA, HIM).
- Security review: "Unusually many patients read: fb:DcGIzIXwxURU0G9L4J5jehluENl1" (raw uid, no staff name) and "Unusual export volume 3". Took ~10 s to load.
- Staff and roles tab shows role descriptions and two-step sign-in ticks, but no list of the hospital's staff in the first screen.

### LT-36 (major) Staff rota empty; a phone number is shown as a staff name
- Screen: `#/rota`
- Actual: "No shifts defined yet", "Nobody is on duty in any ward right now" (root of LT-04 and the Digital twin "Staffing now: Not known"). "Marked off duty (1): 8897298117, doctor: off duty until 2026-09-16 02:02. Not alerted." A 10-digit mobile-looking number is displayed as the staff identity (possible personal data).

### LT-37 (minor) Audit and security: oldest first, raw ids, contradicts System health
- Screen: `#/audit`
- Actual: "Record changes" starts at SEQ 45 from 11 Sept, oldest first, with "Load more" at the bottom; today's actions are not reachable without paging through hundreds of rows. Rows of type "_wardsynq_bed_claim" have blank WHEN and ACTOR; ACTOR elsewhere is "fb:DcGIzIXwxURU0G9L4J5jehluENl1"; timestamps are raw UTC ISO. Emergency declaration reason "bus accidentttttt" left in demo data. Service health says "AI Status ok" and "Notifications Status ok, Delivered 0%, Attempts 6", while Admin > System health says MaiK is Down.
- Sign-in security (`#/security`), Accounts (`#/accounts`), Group overview (`#/group`) opened without errors (read only, nothing changed).

### LT-38 (major) Reports page is a raw data dump, not reports
- Screen: `#/ward/reports` (11 s to load)
- Actual: one 51,000-character page of key/value text: "flow: computedAt: 2026-09-15T16:55:11.422Z ed: arrivals: 0 ... encounterId: wsq-adm-smd-6teqzm-00025-... patientId: opd-pat-smd-6teqzm-00025 ...". Sections Patient flow, Clinical operations, Billing, Claims, Pharmacy, HIM. No tables, charts, date filter, export or report picker (only close/keyboard/refresh buttons). Pharmacy says "dispenseCount: 82" but every drug in "dispenseVolume" is 0.

### LT-39 (minor) Command center and Digital twin: numbers disagree, text glitches, slow
- Command center `#/ward/flowcommand` (12 s): "Beds 83 occupied, 1 admitted with no bed"; ward home at the same time said "85 beds occupied · 2 without a bed"; Map said "84 admitted". Discharge list shows only ward/bed, no patient. Labels in Title Case ("In The Department", "Stays With Nothing Outstanding Right Now").
- Digital twin `#/ward/twin` (19 s): heading "Flow &amp; capacity" (HTML entity shown), "Open Loop(S)", theatre rows run together ("Theatre 10% · 0 min booked" = Theatre 1, 0%), "(detail not available)" after every number, "Radiology backlog 3 not yet reported" while the Radiology board lists 7 requested.

### LT-40 (major) MaiK: "Ask MaiK" fails with a Cloudflare 502 page; screen shows only "bad_response"
- Screen: `#/maik`
- Steps: Choose Test Patient QA-02 (GAS-03), task "Summarise this admission", Ask MaiK.
- Actual: `POST /api/queue/ward/maik-ask` 502 returning a Cloudflare HTML error page; the screen shows the bare code "bad_response" with no explanation or retry. Matches Admin > System health "MaiK Down". Patient picker shows internal ids ("opd-pat-smd-demo-00001") under every name.
- Integration console (`#/ward/integration`) opened fine: nothing held, no sources or destinations registered.

### LT-41 (minor, KNOWN: full-interface translation in progress) Telugu covers only the sidebar and Admin Center tab names
- Steps: header Language -> తెలుగు; visit screens; reload; switch back to English.
- Translated: sidebar navigation (మ్యాప్, వార్డు, బెడ్ బోర్డు, ...), the "Language" label (భాష), Admin Center tab names. Choice survives a reload. Switching back to English restores everything.
- Not translated (0 Telugu words measured): Map tiles and headings, Patients (`#/patients`), ward home (`#/ward/`), Bed board, Emergency department, Critical results board, Staff rota, Audit and security, Admin Center tab contents, the whole ward overlay header ("WardSynQ · Inpatient"). Lab board, Cashier and chart were not measured in Telugu because the sidebar link bounced to the Map (LT-29 navigation note), but they share the untranslated ward overlay.

