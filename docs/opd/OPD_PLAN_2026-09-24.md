# OPD A-to-Z plan (2026-09-24)

Owner ask: fix everything from the OPD + StewardID/Ni-Key audit, make it premium. Built in this order,
one commit per item, full suite green before each push. Resume from the first unchecked item.

Decision taken without asking (reversible): patient IDs use prefix `SMP-` + a check character, so a
patient ID can never be read as a clinic code (`SMD-`).

## Phase 1 - identity (a wrong-patient event is the one bug a hospital never forgives)
- [x] 1. StewardID minted and stored by the SERVER, reserved globally with a create-only write in the
       same commit as the patient (duplicates impossible across devices and clinics). Format
       `SMP-XXXX-XXXXC`, Crockford Base32, Luhn mod-32 check char (catches typos and swaps).
       Client stops minting; it shows what the server returns.
- [x] 2. Server resolve (`GET /identity/resolve`) and revocation (`POST /identity/revoke`) so a lost
       Ni-Key card stops working on every device, not one.
- [x] 3. Client cache holds no PHI: id -> display hint only, expiry, wiped on sign-out.
- [x] 3b. (owner, 2026-09-24) The patient QR does not scan: generate it with the SAME mechanism the
       prescription QR uses, and make one easy find-patient flow for QR, Ni-Key, manual entry and
       barcode - all four land on the same server resolve.

## Phase 2 - OPD bugs
- [x] 4. Front-desk 8s poll never rebuilds over a focused field (same rule as the staff form).
- [x] 5. `bill/shift` + `bill/dispense`: screens and tests, reachability gate green. (Done on main by the session that added them; verified 2026-09-24: gate shows 0 waiting.)
- [x] 6. Reconciliation: find tickets with no visit record and bills with no visit, and flag them.
- [x] 7. One shared renderer for the web console and the app (start with the pulse card).

## Phase 3 - features (premium: ui-ux-pro-max skill first for every screen)
- [x] 8. Appointments: slots, booking -> token on arrival, reminders, reschedule, doctor leave.
       FOUND 2026-09-24 (do not rebuild): booking, no double-booking, blackouts, arrived/cancel/DNA states,
       follow-up requests and recalls already exist in functions/_wardsynq/scheduling.js; portal self-booking
       with published slots and holds in online-booking.js. THE GAP: POST /ward/appointment state "arrived"
       (router ~line 4440) updates the Appointment only - no OPD queue token. Build: on arrived + ok, add the
       patient to the org's pool (Q.addToPool, auto-routes to a single staffed room), ticket carries
       appointmentId + visitType "appointment", return the token. Then items 11 (follow-up loop) and 10 may
       likewise be wiring over scheduling.js recalls, not new builds.
- [x] 9. Money: fee by visit type, free review window, UPI QR, refunds with reason, shift close.
       FOUND 2026-09-24: functions/_clinic_billing.js (order-based: tariff items with kind consultation /
       medication / service / investigation / bed / nursing / visit; buildInvoice, order state machine) +
       _clinic_billing_store.js already have UPI and the cashier shift report (bill/shift, added on main by
       another session - do not rebuild). MISSING: refunds (a reversal with reason, audited, netted in the
       shift report), consultation fee by visit type (new vs follow-up), and a free-review window (follow-up
       within N days of a paid consult at no fee - an org setting). Refund GST treatment is an ACCOUNTANT
       question: build the reversal, flag the GST line for sign-off rather than guessing.
       DONE (pure, test/clinic-billing-money.test.mjs): "refunded" state off "paid" only; consultFee(prices,
       {visitType, freeReviewDays, lastPaidConsultAt, nowMs}) with a stated reason when waived.
       DONE: store refundOrder + POST /bill/refund (billing.charge; test/clinic-billing-refund-route.test.mjs).
       DONE: Refund button (clinic-billing.html loadRefunds), shift report nets refunds (refunds + net).
       DONE: consultFee applied server-side in createOrder (ticket visitType, org wardsynq.freeReviewDays via router,
       last paid consultation). LEFT: (a) route test - follow-up ticket inside window -> unitPrice 0 + "(free review)";
       (b) followupPrice field in the opd.html tariff editor (~line 1800) + validateTariff keeps it.
       (was) REMAINING: consultFee wiring. FOUND: per-doctor consultation fee is a tariff item (kind consultation,
       code CONS-<doctorId>, doctorId) set in opd.html ~line 1800 (bill/tariff). Next: find where that tariff
       becomes an ORDER at visit time (grep CONS- / doctorId in createOrder callers), add an optional followup
       price field to the tariff editor, and price via consultFee with org setting freeReviewDays + the
       patient's last paid consultation (ordersForPatient status paid, kind consultation).
       (was) REMAINING before push: Refund button for PAID orders in clinic-billing.html calling
       bill/refund with a reason prompt; shiftReport nets refundAmount; consultFee wiring below.
       (was) NEXT (wiring): store refundOrder(env, orgId, orderId, reason, actor) paid->refunded, reason required,
       audited; shift report nets refunds; route POST /bill/refund (billing.charge cap); cashier screen
       button in clinic-billing.html; apply consultFee where the consultation order is priced (org setting
       wardsynq.freeReviewDays), showing "free review" on the bill.
- [x] 10. Investigation loop: order -> result back -> patient recalled with the result.
       GROUNDWORK 2026-09-24: OPD ticket status "investigation" exists (_queue_eta.js state machine: from
       in_consultation; can go back to waiting/called/in_consultation). Results are released in
       functions/_wardsynq/lab-result.js (releaseResult; lab-analysers.js files analyser results). Recalls
       exist in scheduling.js (recallIdFor/recallStatus/requestFollowUp). BUILD: when a result is released
       for a patient whose TODAY's OPD ticket is "investigation", move that ticket back to "waiting" at the
       head of its band (like recall) with a note "result ready", and show "Results back: N" on the pulse.
       Find the ticket via today's sessions for the org (same walk as /opd-pulse) matching patientId/mrn.
- [x] 11. Follow-up loop: due -> reminded -> booked -> attended or missed.
       GROUNDWORK 2026-09-25: scheduling.js already has RECALL_TYPE (AppointmentRequest with dueBy), requestFollowUp,
       recallStatus(recall, nowMs) (due/overdue), listSchedule, and an Appointment links the recall it answers
       (requestId). BUILD: listSchedule (or a new read) of open recalls -> pulse "Follow-ups due / overdue" tile
       on both consoles via opd-pulse-model.js; a booked appointment with requestId closes it; overdue ones feed
       item 14 (WhatsApp reminder). Do not rebuild recalls.
- [x] 12. Priority rules with an audited reason on every override.
       DONE 2026-09-25: _queue_eta.js priorityRule (reason sets level: emergency 2; senior/pregnant/disability/child/results/other 1; clear 0;
       other needs a note). setPriority, registration and assign-room all take a reason; change + hash-chained audit row in one guarded
       commit. Registering ahead of the queue now needs the priority right (reception was able to before). Reason sheets in both consoles.
- [x] 13. Degraded desk mode: register and print tokens offline, sync after.
       DONE 2026-09-25: opd-offline-desk.js (window.SMD_OPD_OFFLINE). Online, a desk reserves a series letter for the day
       (POST /offline-series, create-only q_offline_series). Offline, the check-in sheet completes with OA-1, OA-2 ... and a
       printed slip (ward-labels.js kind "token"); the outbox is sessionStorage (dies with the tab: the patient still has
       the slip; IndexedDB is the upgrade). Sync = register then /pool with offlineToken + offlineAt: the ticket keeps the
       slip number and the arrival time, the token is reserved create-only with the ticket (a retry never double-queues),
       the numbered day sequence is untouched. Wired in opd.html and the app front desk (queue.js). Tests:
       test/opd-offline-desk.test.mjs + real browser test/run-opd-offline-desk-ui.mjs (11/11).
       Also bumped ?v= tokens for queue.js/queue.css/opd-pulse-model.js, which items 11-12 changed without a bump.
- [x] 14. Patient live status over WhatsApp (position, ETA, doctor running late).
       FOUND (do not rebuild): _queue_notify.js already sends registered / 5 ahead / 2 ahead / next / complete, WhatsApp
       first with SMS fallback, with the live link (/queue?t=); patient-messaging.js already sends follow-up and
       appointment reminders (consent, quiet hours). THE GAP was "running late": the queue.msg.delayed template (en/hi/te)
       existed and nothing sent it. DONE 2026-09-25: delayDue/doctorAway/clockTime in _queue_notify.js; sent when the
       doctor is away (break/emergency/procedure/meeting/paused) or a patient's estimate slipped 20 min past the one they
       were given (n_etaTold), never to the next patient, 1 per 30 min, 3 per visit, the new time as a clock time.
       session/status now recomputes, so the hall hears it at once. test/queue-notify-late.test.mjs.
- [x] 15. Owner's daily close report.
       DONE 2026-09-25: GET /day-close (analytics.view) = _queue_eta.js dayClose (the pulse, each room: registered/seen/
       no-show/consult median/wait median, patients put ahead by reason, offline check-ins) + today's money from the
       cashier's shift report (takings, refunds, net, by method) + orders not yet paid. The /opd-pulse room walk is now
       one helper (opdDay) both use. opd.html "Close the day": headline, by room, money, still open (with follow-ups to
       book), exceptions, Print (A4). A money read that fails is said, never zero. test/opd-day-close.test.mjs.
       Not built: sending it to the owner on WhatsApp at a set hour (add when asked; the read is the same route).
- [x] 16. Live board over push instead of polling.
       FOUND: no broadcast channel existed (web/native push only raise OS notifications; the one Durable Object relays
       voice). Firestore listeners were ruled out: rules are deployed by hand, and a deploy from this repo could
       overwrite rules changed elsewhere. DONE 2026-09-25: every audited queue change stamps q_live/<hospital> {rev}
       (qAudit, plus the priority and recall commits); GET /live (queue.view, or the wall display's signed token)
       streams {rev} as Server-Sent Events, ends after ~5 min and the client reconnects. opd-live.js reads it with
       fetch (headers; in the app via CapacitorWebFetch, as MaiK streams). The console (no timer before: stale until
       clicked) refreshes on change, never over someone typing; the app's room and front-desk polls stay quiet while
       live (60 s safety pass); the TV board shows a call in ~2 s, 6 s poll kept as fallback.
       test/opd-live.test.mjs incl. end-to-end (real client on the real stream).

## Follow-ups (not OPD features)
- [x] F1. test/wardsynq-theatre-access.test.mjs:207 flakes on slow CI (expected [240,120,50], got [240,119,49.6]; run 36027189862): fixture built from Date.now() at one instant, code under test reads a later Date.now(). Fix: one fixed now for both (or freeze Date), not a looser tolerance. Diagnosis from the AgentConnect session, 2026-09-25.
       DONE: iso() floored each call to the minute separately; one minute-aligned base (BASE_MS) for the whole file.
- [x] F2. test/wardsynq-nurse-staffing.test.mjs fails just after local midnight (draft starts an hour before now). Pin the clock.
       DONE: setup(ctx) pins Date to midday IST of the previous day (ctx.mock.timers) in all six route tests.
- [x] F3. Item 10 matches the OPD ticket to the released result by patientId only; confirm OPD tickets carry the WardSynQ record patient id for record-on hospitals, else add an mrn match.
       CONFIRMED BROKEN, FIXED: the lab releases against the record id (opd-pat-<mrn>) and a desk ticket carries the MRN,
       so the recall never fired. opdResultBack now also matches patientIdForMrn(ticket MRN). Route test that releases a real
       result: test/opd-result-back.test.mjs (item 10 had no route test).
- [x] F4. test/wardsynq-lab-analysers.test.mjs:148 flaked (CI run 36064850173, reported by the AgentConnect session):
       "4.1" matched inside an ISO timestamp's seconds.milliseconds (":24.1xxZ"). Now matched only as a value.

## Owner asks, 2026-09-25 (after the 16 items)
- [x] G1. Offline check-ins survive a CLOSED tab, not only a reload. opd-offline-desk.js now keeps the outbox in
       IndexedDB (db smd-opd-offline): every change is get, change and put in ONE readwrite transaction (two tabs or a
       sync in flight cannot lose a check-in), and issue() resolves only after the write committed, so the sheet shows
       the number only once it is really kept. No IndexedDB (private window): the tab's sessionStorage, and the sheet
       says "keep this tab open" (ward.reg-offline-tab-only, 8 languages). A tab's older sessionStorage outbox is handed
       over on opening. Real browser: test/run-opd-offline-desk-ui.mjs closes the tab and opens a new one (12/12).
- [x] G2. The day close sent to the owner on WhatsApp at a set hour.
       DONE: functions/_day_close.js. Setting org.wardsynq.dayClose {enabled, hour 0-23 on the hospital's clock, mobile}
       via GET/POST /org/day-close-settings (staff.admin; the number only ever returned masked; blank keeps it). The
       stewardmd-api worker's hourly cron (worker/src/index.js, deploys on push) POSTs /ops/day-close-all (admin token):
       due hospitals are claimed create-only in q_dayclose_sent/<hospital>__<date> (one a day), a failure retried on
       later hours up to 3 attempts, every send audited with the number masked. Message = the day close in counts and
       totals, no patient named, over the queue's WhatsApp channel (no SMS: DLT needs a fixed template). POST
       /day-close/send = Send now. Console: "Send it to WhatsApp every day" in Close the day, with today's status.
       NEEDS on the server: FOLLOWCARE_WA_PROVIDER + a template that carries {{text}}, or it says "not set up".
- [x] G3. (found verifying the deploy) stewardmd.in's gate (functions/_middleware.js) 404'd opd-pulse-model.js,
       opd-offline-desk.js, opd-live.js and ward-labels.js, so on stewardmd.in/opd the pulse (item 7), offline desk (13)
       and live boards (16) silently did not load (wardsynq.com was fine). Allowlisted; test/public-page-scripts-served
       asks the real gate for every asset each public page loads, so a fifth one fails in CI.
