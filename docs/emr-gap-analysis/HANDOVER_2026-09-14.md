# WardSynQ handover, 2026-09-14 (end of day)

Read this first after a context reset, then the tail of `docs/emr-gap-analysis/P0_CURRENT_STATE.md` and
`docs/emr-gap-analysis/P2_EXIT_EVIDENCE.md`.

## Where things stand
- P0, P1, P2 engineering complete and live (wardsynq.com site, stewardmd.in functions on push to main).
- Last shipped: OPD token numbers (a019afd1), owner acknowledgement of a legitimate audit restore (98f6140b),
  portal gaps closed (queue status, released documents, full discharge summary; 04cb2d4e), audit chain anchored
  in KV hourly (8a6710bf), security scan in CI (scripts/security-scan.mjs), P2 exit evidence doc.
- Regression at last run: 6641 tests, 6640 pass, 0 fail, 1 skipped. `node scripts/wardsynq-reachability.mjs`:
  "Every route has a screen and a test." (23 screens after excluding dist-wardsynq). Security scan: 0 findings,
  16 public-by-design, 2 dependency WARNs.
- Worktree: /Users/diwakarkumar/Developer/StewardMD/.claude/worktrees/wardsynq-p0, branch wardsynq-product.
- Live tokens: ward.js site79, admin.js 26, portal.js 3 (check wardsynq/site/index.html and portal.html).

## Status of the owner answers (updated 2026-09-14 late night)
Built and live (details in P0_CURRENT_STATE.md "night, 3" to "night, 10"; plan in OWNER_ANSWERS_BUILD_PLAN.md):
D2, D4, D5, D6 (language files ready; Antigravity translates per docs/wardsynq/TRANSLATION_BRIEF_ANTIGRAVITY.md),
D7, D8, D9, D10 (sign-off screen; owner has not signed any item), D11, D13, D14, G1-G14, S2, S4, S5, S7,
S3 P0 (server alert path, worker */5 cron) and P1 (app client, needs app update), S6 A1 profile, A3 ABDM v3 merge,
O1 (Pro gate removed from Ward Sync login).
Open: S1 bucket name (owner); S3 P2-P6 (native channel, hospital home in app, WebView parity, server
deterioration alerts); S6 A2-A7 (blocked on India hosting, AWS move ~2026-09-28); NHCX onboarding; native rebuild
or OTA for app pages (queue.js, patient-register.js, ward.js, native-push.js, index.html); owner confirmation of
external ABDM invoices filed as clinical notes and of level-2 alerts going to every nurse on duty (no
nurse-in-charge marker); G15 old muse-* worktrees (unstaged copies of merged work) to delete by hand.
In progress at time of writing: s3-p1-followups (notice route orgId check, queue.js/discharge.js credentials,
account sign-out unbind, live no-phone list).

## Owner answers (2026-09-14, binding; supersede the recommendations below)
- S1 bucket name: owner needs time. G12 second anchor must not wait on it (use a non-D1, non-KV store).
- S2 payments: WardSynQ is sold to many hospitals; build a pluggable payment-gateway connector so any gateway plugs in.
- S3 notifications: through the StewardMD app. Integrate Ward Sync (in the app) and WardSynQ (web) as ONE in the
  phone app. No existing functionality of Ward Sync (app) or wardsynq.com (web) may be lost.
- S4 payers: same as S2, a pluggable payer/TPA connector.
- S5 imaging: be a plug; anyone with DICOM (DICOMweb / PACS) can integrate.
- S6 ABDM: registrations are hospital-specific. Build the ABDM integration; each hospital later uses its own
  facility ID. (Another session owns ~/Developer/abdm on feat/abdm-v3-reconcile: reuse, do not conflict.)
- S7 AI with PHI agreement: Vertex. Go with it.
- D1 voice: browser on-device speech only, on the computer where the site is open. No cloud.
- D2 A: upgrade @xmldom/xmldom and brace-expansion, retest.
- D3: no paid penetration test for now (no budget). Keep docs/PENETRATION_TEST_PLAN.md for later.
- D4 B: group counts from a hospital-published snapshot (WardSynQ is deployed per hospital, Epic-style).
- D5 B: structured per-result withholding (owner: "do which is better, not easy").
- D6: portal languages English, Spanish, Telugu, Hindi, Bengali, Kannada, Tamil, Malayalam. Antigravity writes the
  translations. We prepare per-language files so its work cannot conflict, then test and improve once done.
- D7 B: token scope per department, integrated with StewardMD OPD.
- D8: follow standard guidelines (WCAG contrast, conventional clinical colour meaning).
- D9 B: FHIR R4B/R5 now.
- D10: clinical seed data sign-off by Dr Manoj Kurmana.
- D11 A: per-hospital clinical settings template.
- D12: AWS migration in 2 weeks (around 2026-09-28). Still no new Cloudflare coupling in domain logic.
- D13, D14: not answered; applied the recommendation (yes to no-show recall with the same token; yes, prefixes
  required for department scope).
- G1-G15: all approved.

## Owner answers, 2026-09-15 (binding)
- Ward alerts: everyone ON DUTY on the patient's ward (nurses, residents, consultant); nobody off duty. Nurses and
  residents can turn themselves off duty in the StewardMD app. (branch ward-duty-team; supersedes nurse-only level 2)
- Staff workstation: translate navigation labels (clinical screens stay English).
- Translations: owner uses Google Translate via Gemini CLI; English is the main language; other languages optional;
  English must always stay (safety). Printouts: English complete and authoritative, second language optional
  alongside (branch bilingual-prints, per-hospital setting default off).
- No partial languages: every offered language must be fully translated (branch i18n-full-gemini).
- Patient with no ward: alert the doctor the patient is admitted under and the resident on duty (not hospital-wide
  cover). Residents taken from the admitting doctor's department when known, else hospital-wide on-duty residents
  (orchestrator's reading; branch no-ward-alert-cover). Interns stay out of the ward team; named contacts are still
  reached by name when off duty (defaults, owner not yet asked to change them).

## Owner answers, late 2026-09-14 (binding)
- Portal languages: add Marathi and keep Spanish (nine: en, es, te, hi, bn, kn, ta, ml, mr).
- External ABDM invoices are stored as clinical/document records for now and MUST NOT automatically become
  WardSynQ billing transactions.
- Level-2 alerts go to every nurse marked ON DUTY in the affected ward until a proper Nurse-in-Charge
  role/assignment exists.
- Both rules explicit, auditable, tenant-scoped and easy to change later (branch policy-abdm-invoice-level2).

## Owner answers to the S3/S6 design questions (2026-09-14, binding)
- O1 yes: hospital staff (WardSynQ and GHIS hospital doctors) never meet the personal Pro paywall in their hospital
  workplace. Done: Pro gate removed from POST /api/ghis/login (test/ghis-login-no-pro-gate.test.mjs).
- O2: use StewardMD's existing push notifications only; no Apple Critical Alerts application.
- O3: push text never carries the patient name (or MRN); bed number and ward are allowed.
- O4: SMS fallback approved; owner has DLT registration on VilPower and SMS via 2Factor.
- O5: default escalation ladder approved by Dr Manoj Kurmana (ordering + on-duty doctor; supervisor + nurse in
  charge at 30 min; named contacts at 60 min).
- A1: shared StewardMD ABDM bridge, each hospital links its own facility (no own-bridge option).
- A2 yes: hold all production ABDM traffic until India-region hosting from the AWS move exists.
- A3: this session merges feat/abdm-v3-reconcile into wardsynq-product.
- A4: records received under consent stay in the chart, marked as received under that consent, after withdrawal.
- A5 yes: billing and front desk staff may create new ABHA numbers (Aadhaar OTP) as well as verify.

## The user is answering these (numbered; wait for answers, then act)

### Setup only the owner can supply
- S1 Document bucket: correct R2 bucket name (DOC_S3_* secrets exist; current name rejected InvalidBucketName).
- S2 Payment provider + credentials (recommended Razorpay).
- S3 Critical-result notification channel (SMS / WhatsApp / push + provider).
- S4 Insurance/TPA payer endpoints + credentials (adapter tested with mocked fetch only).
- S5 PACS viewer URL template (study UID + accession).
- S6 ABDM HFR facility ID and doctors' HPR IDs.
- S7 Which AI providers have a PHI data agreement (wardsynq.ai.phiApproved; default none).

### Decisions (my recommendation in brackets)
- D1 Voice typing cloud speech? (A: on-device only, as now)
- D2 Upgrade @xmldom/xmldom and brace-expansion? (A: yes, retest incl. app build)
- D3 Book penetration test before go-live? (A: yes; plan in docs/PENETRATION_TEST_PLAN.md)
- D4 Group counts: system read (A, as now) vs hospital-published snapshot (B)
- D5 Full summary withholding per section (A now) vs structured per-result (B later)
- D6 Portal Hindi/English switch across all sections + native Hindi review (A; who reviews?)
- D7 Token scope default whole hospital (BUILT as recommended)
- D8 Colours: recording dot red, admin status pills, portal Revoke red (keep all)
- D9 FHIR R4B/R5 deferred (A) [superseded: owner answered B, built 2026-09-14, see docs/FHIR_STRATEGY.md]
- D10 Who signs off unapproved clinical seed data (allergy classes, dose rules)?
- D11 Per-hospital clinical settings template (highAlertDrugs, antibiotics, orderVerifyWithinHours,
  edReassessMinutes, criticalEscalation, patientAccess.enabled, rpoMinutes) (A: template)
- D12 AWS migration still planned (~2 weeks)? confirm date; no new Cloudflare coupling in domain logic
- D13 Allow recall of a no-show ticket with the same token? (yes; no_show is terminal in _queue_eta.js today)
- D14 Require prefixes when token scope is per department? (yes)

### Gaps I can fix (user says "fix all" or picks)
- G1 Outbox scan capped at newest 200: add a status index.
- G2 Offline: post-reconnect conflict review screen.
- G3 Chain the Firestore org/staff audit (q_audit) like the clinical audit.
- G4 Live audit chain not yet observed (no production writes since 11:10Z deploy); hourly check armed.
- G5 Chart Documents: show which versions were released to the portal.
- G6 Immunization record type + screen + IPS section.
- G7 Trends: ward-by-ward stay history for transfers; per-day bed counts for past occupancy.
- G8 Webhooks: edit address; proper per-endpoint delivery log (currently newest 1000 hospital-wide).
- G9 Bulk export: Group/$export, POST kick-off, admin download buttons.
- G10 FHIR Subscription create over FHIR + $status.
- G11 Out-of-assignment reads: ward history, email-to-uid matching, clickable evidence.
- G12 Second outside anchor (e.g. backup bucket after S1) against a D1+KV attacker.
- G13 Keep scroll position / focus after actions (unless a warning must be seen).
- G14 Tablet two-pane for the single-patient nursing panel.
- G15 Tidy: dead `tr.warn` CSS in rota.js/security.js; remove old muse-* and agent-* worktrees; confirm the
  security-scan CI job's first run is green.

## How work is run here
- Rules from the owner: no AWS migration yet; no new Cloudflare coupling in domain logic; server-side authz +
  audit + negative-auth tests; reachability green; clinical safety strict; an unloaded list never looks empty;
  no false greens; plain non-technical language to the user; no emoji/em dash in app text; never add env
  secrets/vars (bindings at limit); stage files explicitly; never force-push.
- After each change: node --check; `node --test --experimental-test-module-mocks --test-concurrency=4
  test/*.test.mjs` (0 failing; redirect to a file and grep "ℹ tests/pass/fail"); reachability; bump ?v= tokens;
  commit with trailers `Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>` and
  `Claude-Session: https://claude.ai/code/session_01DKrDwaxMXTx69zQNFMozxX`; `git fetch origin main` + merge;
  `git push origin wardsynq-product:main` and `git push origin wardsynq-product`;
  `bash scripts/build-wardsynq-site.sh --deploy`; verify live with curl; append to P0_CURRENT_STATE.md.
- Server deploy status: `bash /Users/diwakarkumar/.claude/jobs/d1986003/tmp/wait-pages.sh <sha>`.
- Builders: Agent subagent_type genius-opus, isolation worktree, brief = the common rules file
  `/Users/diwakarkumar/.claude/jobs/d1986003/tmp/common-brief.md` + the task. Merge their branch yourself.
- Muse (user wants it used as a co-worker; check every 10 minutes while it runs):
  `git worktree add -q ../muse-<task> -b muse/<task> HEAD`, then
  `/Users/diwakarkumar/.local/bin/muse exec --model muse-spark-1.3 --reasoning-effort xhigh --yolo --workspace <abs worktree> --prompt-file <brief>`
  (run_in_background). Rules file for Muse: `/Users/diwakarkumar/.claude/jobs/d1986003/tmp/muse-common.md`.
  Muse leaves changes unstaged; this session cannot run git inside its worktree, so bring changes over with cp
  (files unchanged on our side since its base) or `git show <base>:<path>` + `diff -u` + `patch`. Muse is slow
  at its final full-suite self-check: once its targeted tests pass, it is fine to stop it and verify here.
  Muse has a usage quota (resets reported in its log as a 429) and can be killed on low memory; relaunch.
- Worktree guard: no heredocs/complex pipelines in Bash; write helper scripts with Write, run, delete.
- Production D1 (stewardmd-connect) schema changes: apply with `npx wrangler d1 execute stewardmd-connect
  --remote --file ...` BEFORE pushing code that depends on them; test first with `--local --persist-to <tmp>`.
- Crons are session-only and vanish on restart. Armed at time of writing: hourly audit-chain live check
  (query: new connect_audit_event rows since 2026-09-14T11:10:00Z vs wardsynq_audit_chain count).
