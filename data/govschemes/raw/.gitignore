# Raw government source documents: fetched on demand, never committed (~45MB of PDFs/workbooks).
# Provenance lives in MANIFEST.tsv (sha256) + the D1 sources table (URL, retrieved_ts).
*
!MANIFEST.tsv
!.gitignore
