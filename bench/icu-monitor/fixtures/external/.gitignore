# Third-party monitor photo datasets (CC BY 4.0 as tagged by their Roboflow uploaders; ownership not
# stated). INTERNAL BENCHMARK USE ONLY: never committed, never shipped. Credit: see README.md.
*
!.gitignore
!README.md
# our own value labels (no images): kept so the labelling work survives
!cases/
!cases/*-drafts.tsv
