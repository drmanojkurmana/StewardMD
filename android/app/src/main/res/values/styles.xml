<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
        <!-- White window behind the WebView so the moment between the system splash and the white
             HTML splash stays white (never a dark flash), even in dark mode. -->
        <item name="android:windowBackground">@android:color/white</item>
    </style>


    <!-- Android 12+ system splash. Without these attrs the OS shows the round launcher icon on a
         plain background first. We brand it to match the app's white HTML splash: white background
         + the dark-green StewardMD monogram (@drawable/splash_icon, padded so it isn't clipped to
         a circle). The Capacitor navy splash is disabled (launchShowDuration 0) so this hands off
         straight to the white HTML splash (logo, name, loading, "created by MaiK") — no dark screen
         and no teal badge. -->
    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@android:color/white</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_icon</item>
        <item name="postSplashScreenTheme">@style/AppTheme.NoActionBar</item>
        <item name="android:background">@android:color/white</item>
    </style>
</resources>