<?xml version="1.0" encoding="utf-8"?>
<!-- RECORD_AUDIO is declared by the host app (android/app/src/main/AndroidManifest.xml); the
     plugin requests it at runtime via @CapacitorPlugin(permissions=...) in WhisperPlugin.kt. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android" />
