TASK: Add a hospital-level RADIOLOGY board to WardSynQ.

REPO: /Users/diwakarkumar/Developer/StewardMD/.claude/worktrees/wt-rad (branch synergy/radboard)
Work ONLY in that directory. Do not touch any other worktree.

WHY: WardSynQ has an imaging BACKEND but no hospital-level radiology screen. A radiographer or
radiologist who signs in has nowhere to land. Every other department (ED, theatre, pharmacy stock,
critical results) has a board; radiology does not. You are adding the missing screen, NOT new
backend.

THE BACKEND ALREADY EXISTS. Do not invent endpoints. Read
functions/api/queue/[[path]].js and use exactly these (all under /api/queue/ward/, all take
?orgId=<id>):
  GET  imaging-worklist      - imaging requests and their state
  POST report-imaging        - file a radiology report against a request
  GET  criticals             - critical findings needing acknowledgement
Read each handler before you call it and match its real parameter names and response shape.
The client helpers are apiGet/apiPost in ward.js; they already add auth.

WHERE THE CODE GOES: ward.js, following the EXISTING board pattern exactly. Study
`critsBoardOpen` (~line 3654), `loadIntegration` (~line 3662) and the `critsboard` case in the
dispatch block (~line 4929) first, then copy that shape:
  1. `function radBoardOpen() { st.view = "radboard"; ...; paint(); loadRadBoard(); }`
  2. `function loadRadBoard()` - follow loadIntegration's error discipline: record EACH failed read
     by name. "nothing requested" and "could not tell" must never render the same, because one of
     them means an unreported scan nobody is looking at.
  3. a render function returning an HTML string, same helpers as the other views (esc(), ms(),
     w-card / w-btn / w-st CSS classes that already exist in ward.css - add no new CSS file)
  4. add `if (cmd === "radboard") { radBoardOpen(); return; }` to the dispatch block
  5. add "radboard" to the HOSPITAL_ACTS array (~line 5134)
  6. render the view where the other `st.view` branches are handled

WHAT THE BOARD MUST SHOW (this is a working clinical screen, not a stub):
  - requested studies awaiting acquisition, with patient NAME and MRN (never a bare record id),
    modality, and priority (STAT and urgent visually distinct from routine)
  - studies acquired and awaiting a report, with how long they have been waiting
  - reported studies, most recent first
  - open critical findings, most urgent first, visually distinct
  - a way to file a report against a waiting study (calls report-imaging)
  - counts per section in the section headers, and a refresh control
Empty states must say which thing is empty ("No studies awaiting a report"), never a bare "None".
A failed read renders as a named error, not as an empty section.

NOTE, worth getting right: a plain radiograph has NO specimen. If the backend returns a
specimen/collection state for an imaging request, do not render a "Collect" action for it. An
imaging order is acquired, not collected.

HARD RULES:
  - ES5 only. `var`, `function`. No arrow functions, no let/const, no template literals, no
    optional chaining. This is a buildless IIFE bundle; modern syntax breaks the older browsers
    the wards run and is not transpiled.
  - Escape every piece of server data through the existing esc() before putting it in HTML.
  - No em-dashes in any user-facing string.
  - Do not modify any file other than ward.js.
  - Do not touch the existing chart, ward list, or any other view.
  - `node --check ward.js` must pass.

VERIFY BEFORE YOU FINISH:
  1. node --check ward.js
  2. node --test --experimental-test-module-mocks test/*.test.mjs  (must stay at 0 failures)
  3. grep your new function names back out of ward.js to confirm they are present and wired
COMMIT on branch synergy/radboard with a message explaining what was missing and why the screen
is shaped the way it is. Do not push. Do not merge.

REPORT BACK: the function names you added, the endpoints you wired, and the exact test counts.
