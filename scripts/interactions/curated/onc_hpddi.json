{
  "_comment": "ONC / NLM High-Priority Drug–Drug Interaction list, curated (transcribed, not copied verbatim; U.S. Government work — public domain). Most ONC-derived pairs are already carried verbatim in legacy_rules.json from the pre-pipeline ruleset; add NEW ONC-derived rules here (same schema as mechanism_rules.json) and they will be merged + deduped by build_rules.py. Empty today because the migrated legacy set already covers the ONC pairs shipped so far.",
  "rules": []
}
